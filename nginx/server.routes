location /api {
    proxy_pass   http://127.0.0.1:3000;
    # allow unlimited uploads. api server takes care of limit
    client_max_body_size 0;
}

location / {
    root   ../webadmin/;
    index  index.html index.htm;
}
